// xyGraph: XYGraph figure
// x,y,width,height: geometry
// color, background: Colors
// readable: input of IReadableSamples  (http://toem.de/index.php/projects/impulse/scripts/reference)

// simple modifications
/*
xyGraph.primaryXAxis.setAutoScale(false);
xyGraph.primaryXAxis.setTitle("my x");
xyGraph.primaryYAxis.setAutoScale(false);
xyGraph.primaryYAxis.setTitle("my y");
xyGraph.primaryYAxis.setDashGridLine(true);
*/

// add a trace - use "Manual Fill" flag to disable default traces
/*
importPackage(Packages.org.eclipse.nebula.visualization.xygraph.dataprovider);
importPackage(Packages.org.eclipse.nebula.visualization.xygraph.figures);

        // Create Simulation Data
        int count = 0;

        final short[] simuData = new short[DataWidth * DataHeight * 2];
        int seed = count++;
        for (int i = 0; i < DataHeight; i++) {
            for (int j = 0; j < DataWidth; j++) {
                int xn = j - DataWidth;
                int yn = i - DataHeight;
                int p = (int) Math.sqrt(xn * xn + yn * yn);
                simuData[i * DataWidth + j] = (short) (Math.sin(p * 2 * Math.PI / DataWidth + seed * Math.PI / 100) * 100);
            }
        }
        intensityGraph.setDataArray(simuData);
